{
  "version": 2,
  "public": true,
  "builds": [
    { "src": "dist/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/search", "dest": "/api/search" },
    { "src": "/(.*)", "dest": "/dist/$1" }
  ]
} 